"use client";import {FormEvent,useState} from "react";
export default function Login(){const[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const password=new FormData(e.currentTarget).get("password");const r=await fetch("/api/admin-login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password}),cache:"no-store"});if(r.ok){window.location.replace("/admin");return}setLoading(false);setError("Mot de passe incorrect")};return <main className="loginPage"><form onSubmit={submit}><div className="brand"><img src="/lachger-logo.png" alt=""/><span><b>LACHGER CAR</b><small>Espace administrateur</small></span></div><p className="eyebrow">ACCÈS SÉCURISÉ</p><h1>Bienvenue</h1><p>Entrez le mot de passe pour gérer les véhicules et les réservations.</p><label>Mot de passe<input autoFocus required type="password" name="password" placeholder="••••••••"/></label><button className="primary" disabled={loading}>{loading?"Ouverture de l’espace…":"Se connecter"}</button>{error&&<div className="loginError">{error}</div>}<a href="/">← Retour au site</a></form></main>}
